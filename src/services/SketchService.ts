import { ISketchService } from "./ISketchService";

export class SketchService implements ISketchService {
    
    CreateWebBrowserWindow(): void {
        throw new Error("Method not implemented.");
    }
}